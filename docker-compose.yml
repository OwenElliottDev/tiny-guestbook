services:
  guestbook:
    image: owenelliottdev/tiny-guestbook:latest
    container_name: tiny-guestbook
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
